/**
 * Defines different types of modes of operations that user can perform on saved search
 */
 export enum SavedSearchMode {
    CREATE = 'create',
    RENAME = 'rename',
    DUPLICATE = 'duplicate',
    UPDATE = 'update'
}
