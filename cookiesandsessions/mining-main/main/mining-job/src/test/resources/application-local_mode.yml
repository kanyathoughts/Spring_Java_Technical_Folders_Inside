spring:
    datasource:
        url: jdbc:h2:mem:testdb
        driverClassName: org.h2.Driver
        username: sa
        password: password
    sleuth:
        async:
            enabled: false
job-api:
    cluster:
        name: local-mode
        mode: standalone
        network:
            port: 5710
            portAutoIncrement: true